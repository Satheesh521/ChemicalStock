# Chemical Stock Maintenance App

This is a React Native and Expo application built for industrial chemical stock and inventory management.

## Features
- Track Stock In and Stock Out balances
- 3 Decimal precision for accurate calculations (kg, g, ml)

## How to Run
1. Clone the project
2. Run `npm install` to install dependencies
3. Run `npx expo start` to start the development server
4.
